<div align="center">

# SimpleAuthApp  
Простое веб-приложение с регистрацией, авторизацией и защитой reCAPTCHA

</div>

## О проекте

**SimpleAuthApp** — это минималистичный PET-проект, созданный специально для демонстрации навыков QA и автоматизации тестирования.

Основные возможности:
- Регистрация пользователей
- Авторизация с проверкой сессии
- Обновление имени в личном кабинете
- Защита от SQL-инъекций (параметризованные запросы)
- Google reCAPTCHA v2 («Я не робот»)
- Клиентская и серверная валидация полей
- Полноценный набор автотестов на PyTest (позитивные + негативные сценарии)

Проект отлично подходит как демонстрация для резюме Junior/Middle QA Engineer (автоматизация + API-тестирование + безопасность).

## Технологический стек

- Backend: Flask 3.0
- База данных: SQLite
- Хэширование паролей: Werkzeug
- Защита от ботов: Google reCAPTCHA v2
- Тестирование: PyTest + Flask test client
- Отчёты: pytest-html (опционально)


## Структура проекта
```
pet_project/
├── app.py                  # Основной файл приложения (Flask routes, логика регистрации/авторизации, CAPTCHA)
├── requirements.txt        # Зависимости (Flask, Werkzeug для хэширования паролей)
├── database.db             # SQLite база данных (создаётся автоматически)
├── README.md               # Описание
├── templates/              # HTML шаблоны
│   ├── base.html           # Базовый шаблон (для наследования)
│   ├── register.html       # Форма регистрации
│   ├── login.html          # Форма авторизации
│   └── dashboard.html      # Дашборд (профиль пользователя)
├── static/                 # Статические файлы
│   ├── style.css           # Простой CSS для стилизации
│   └── script.js           # JS для клиентской валидации полей и CAPTCHA
└── tests/                  # Директория для автотестов на PyTest
    └── test_app.py         # Примеры автотестов (unit и integration)
```

## Как запустить проект локально

### Предварительные требования

- Python 3.10+
- Git
- Установленный браузер (рекомендуется Chrome)

### Шаг за шагом

1. Клонируйте репозиторий

```bash
git clone https://github.com/Sir9Niks/SimpleAuthApp.git
cd SimpleAuthApp
```
Создайте и активируйте виртуальное окружение
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# macOS / Linux
source venv/bin/activate
```
Установите зависимости
```bash
Bashpip install -r requirements.txt
```
Важно! Настройте свои ключи reCAPTCHA
```bash
Откройте app.py и замените тестовые ключи на свои (получите здесь: https://www.google.com/recaptcha/admin/create)
PythonRECAPTCHA_SITE_KEY = '6Le...ваш_site_key'
RECAPTCHA_SECRET_KEY = '6Le...ваш_secret_key'
```
Запустите приложение
```bash
python app.py
```
Приложение будет доступно по адресу:
→ http://127.0.0.1:5000

## Как запустить автотесты
```bash
# Самый надёжный способ
python -m pytest tests -v
```

Основные проверяемые сценарии
```bash
✅ Успешная регистрация
✅ Успешный вход
✅ Обновление имени пользователя
✅ Выход из системы
❌ Пустые поля
❌ Некорректный email
❌ Короткий пароль
❌ Дубликат пользователя/email
❌ Неправильный пароль
❌ Непрохождение reCAPTCHA
🛡️ Попытки SQL-инъекций
```
